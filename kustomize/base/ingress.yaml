apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: argo-combined-app
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - kind: Rule
      match: Host(`argo-combined-app.local`) || Host(`argo-combined-app.build-00.openticket.tech`)
      services:
        - name: argo-compbined-app
